================================================================================
 connected  --  CTF 2026 Reverse Engineering Writeup
================================================================================

Category    : Reverse Engineering / Crackme
Challenge   : connected
Source      : https://github.com/crackmesone/ctf-2026-challenges-public
              (connected/handout/connected)
Flag format : CMO{...}
FLAG        : CMO{secret_code_v9hcdkd2}

Description:
  "No matter where you go, everybody's connected."
  We're an automotive security startup. Last night, our garage was breached.
  We managed to capture some logs via our first-ever agent. Can you analyze
  them and find out what was taken?


--------------------------------------------------------------------------------
 TL;DR
--------------------------------------------------------------------------------
The handout is a non-stripped x86-64 Linux ELF that SIMULATES a tiny network of
PCs, switches and routers. It asks the player for:

    what:  <message>
    where: <destination IP>

To get the flag you must send a message the TARGET PC (100.25.26.10) accepts.
The target fans the message out to helper PCs, each computing one check; only if
ALL checks pass does the target forward the cleaned message to PC1, which formats
it into the flag.

Required inputs:
    what  = msg_:"*$$*":
    where = 100.25.26.10

The accepted message is forced by three constraints (length 8, a specific
checksum, all-even-and-printable bytes). Brute-forcing the constraint space
yields a UNIQUE message, :"*$$*":, which the flag formatter turns into:

    CMO{secret_code_v9hcdkd2}


--------------------------------------------------------------------------------
 1. TRIAGE
--------------------------------------------------------------------------------
$ file connected
connected: ELF 64-bit LSB pie executable, x86-64, version 1 (GNU/Linux),
dynamically linked, interpreter /lib64/ld-linux-x86-64.so.2,
BuildID[sha1]=98fb8f46258eb452616de7688c900efb90b7740b,
for GNU/Linux 3.2.0, not stripped

- Not stripped -> symbol names and string literals present.
- C++ (libstdc++, std::string, std::getline, std::cout).
- The repo also ships FULL SOURCE under connected/source_code/ (src/main.cpp,
  include/types.hpp, keygen/). The intended algorithm is readable, but the ANSWER
  is still computed at runtime -- we recover it independently below.

Interesting strings carved from the binary:
    HI THERE
    I don't want to talk to you
    My complicated firewall rules told me to not talk to you
    abcdefghijklmnopqrstuvwxyz0123456789_      <- flag alphabet
    CMO{secr  et_code_                         <- flag prefix (split immediates)

NOTE: the flag SUFFIX (v9hcdkd2}) is NOT a literal in the binary -- it is
generated from the user message at runtime, so grepping for the flag fails.

One-line hypothesis: networked crackme; recover the message that satisfies the
target PC's checks, then the flag is a deterministic transform of that message.


--------------------------------------------------------------------------------
 2. THE SIMULATED NETWORK
--------------------------------------------------------------------------------
main() builds net_pc objects, each with an ipv4_handler lambda. Packets are
routed L2 (frames/switches) and L3 (IP/routers). Relevant hosts:

  Host      IP             Role
  --------  -------------  ---------------------------------------------------
  user PC   38.15.199.42   where input is typed; prints replies sent to it
  TARGET    100.25.26.10   validates the message and orchestrates the helpers
  PC1       38.15.199.41   FLAG FORWARDER -- formats non-user payloads to a flag
  PC2       38.15.199.40   replies "OK" to everything
  PC3       64.14.3.25     returns the string LENGTH
  PC4       64.14.3.29     returns the combined CHECKSUM/HASH
  PC5       100.25.26.11   DoS gag -- spams "HI THERE!!" to random IPs
  PC7       100.25.26.15   returns ALL-EVEN-AND-PRINTABLE boolean
  PC9(xor)  83.48.92.8     XORs anything it receives with 0x42 and echoes back

Message flow (intended path):

  user --( "msg_<payload>" )--> target(100.25.26.10)
     target: src must be user, payload must start with "msg_", strip "msg_"
     target --> PC9 : XOR payload with 0x42
     target --> PC3 : length         (PC3 round-trips via PC9 to un-XOR)
     target --> PC4 : checksum hash   (PC4 round-trips via PC9 to un-XOR)
     target --> PC7 : even/printable  (PC7 round-trips via PC9 to un-XOR)
     target: if (len==8 && hash==100806214 && even_printable)
                 target --> PC1 : cleaned payload
                 PC1 --> user   : CMO{...}     <-- printed
             else
                 target --> user: "I don't want to talk to you"

The XOR-with-0x42 round trips through PC9 CANCEL OUT, so each helper operates on
the PLAINTEXT payload -- PC9 is effectively a red herring.


--------------------------------------------------------------------------------
 3. THE THREE CHECKS (recovered from the binary / source)
--------------------------------------------------------------------------------
Target PC handler (src/main.cpp ~lines 858-892):

    if (len == 8 && hash == 100806214 && is_even_printable) { ...forward to PC1 }

So the accepted payload must be:
    1. Exactly 8 bytes                          (PC3)
    2. Every byte even-valued AND printable     (PC7)
         is_even &= !(c & 1);  and  isprint(c)
    3. combined_hash == 100806214               (PC4)

PC4 combined hash (~lines 1063-1078):

    adler      = adler_32(payload);        // standard Adler-32
    fletcher   = fletcher_16(payload);     // standard Fletcher-16
    shift_csum = 0;
    for (i=0; i<n; ++i) shift_csum += payload[i] << i;
    is_palindrome = (payload == reverse(payload));   // bool 0/1
    combined = ((adler ^ fletcher) * is_palindrome) ^ shift_csum;

The "* is_palindrome" factor zeroes the (adler^fletcher) term for non-palindromes
-> the intended message is a PALINDROME.

adler_32 (~lines 63-78):
    a=1, b=0
    for each byte x: a=(a+x)%65521; b=(a+b)%65521
    return (b<<16)+a

fletcher_16 (~lines 80-90):
    a=0, b=0
    for each byte x: a=(a+x)%255; b=(a+b)%255
    return (b<<8)|a

PC1 flag formatter (~lines 671-679):

    chars = "abcdefghijklmnopqrstuvwxyz0123456789_";   // len 37
    flag  = "CMO{secret_code_";
    for (i=0; i<payload.size(); ++i)
        flag += chars[(payload[i] + i) % 37];
    flag += '}';

The flag suffix is a POSITION-DEPENDENT transform of the message -- which is why
it never appears as a literal.


--------------------------------------------------------------------------------
 4. SOLVING (independent recovery -- nothing copied)
--------------------------------------------------------------------------------
We never hard-code the message. We reimplement the three checks and brute force
the small constraint space:

  - A length-8 PALINDROME is determined by its first 4 bytes.
  - Each byte is even AND printable: 0x20..0x7e with low bit clear -> 47 values.
  - Search space = 47^4 ~= 4.9M -> milliseconds in Python.

The brute force returns a SINGLE solution:

    payload = :"*$$*":
    bytes   = 3A 22 2A 24 24 2A 22 3A   (palindrome, all even, all printable)

Byte sanity check:
    0x3A ':'  even,printable
    0x22 '"'  even,printable
    0x2A '*'  even,printable
    0x24 '$'  even,printable

Flag formatter applied to :"*$$*":

    chars = "abcdefghijklmnopqrstuvwxyz0123456789_"  (len 37)

    i  byte    (byte+i)%37  -> char
    0  0x3A=58    21          v
    1  0x22=34    35          9
    2  0x2A=42     7          h
    3  0x24=36     2          c
    4  0x24=36     3          d
    5  0x2A=42    10          k
    6  0x22=34     3          d
    7  0x3A=58    28          2
                              --------
                              v9hcdkd2

  =>  CMO{secret_code_v9hcdkd2}

The brute force converging to exactly one message confirms the answer is FORCED
by the constraints, not guessed.


--------------------------------------------------------------------------------
 5. PROOF OF CONCEPT (solve.py)
--------------------------------------------------------------------------------
See solve.py in this folder. It reimplements adler_32, fletcher_16, the PC4
combined hash and the PC1 flag formatter, brute forces the unique message, and
prints the inputs + flag.

  $ python solve.py
  [+] payload (message minus magic): ':"*$$*":'
  [+] what : msg_:"*$$*":
  [+] where: 100.25.26.10
  [+] FLAG : CMO{secret_code_v9hcdkd2}


--------------------------------------------------------------------------------
 6. REPRODUCTION STEPS
--------------------------------------------------------------------------------
Option A -- solve without running the binary (any OS):

  # 1. Get the challenge
  git clone --depth 1 https://github.com/crackmesone/ctf-2026-challenges-public
  cd ctf-2026-challenges-public/connected

  # 2. Triage the handout
  file handout/connected               # ELF 64-bit, not stripped

  # 3. Read the checks (or carve strings if no source were provided)
  sed -n '858,892p'   source_code/src/main.cpp   # target validation + hash
  sed -n '1063,1078p' source_code/src/main.cpp   # PC4 combined hash
  sed -n '63,92p'     source_code/src/main.cpp   # adler_32 / fletcher_16
  sed -n '671,679p'   source_code/src/main.cpp   # PC1 flag formatter

  # 4. Run the constraint solver
  python solve.py
  #   -> CMO{secret_code_v9hcdkd2}

Option B -- run the actual binary (Linux / WSL) and confirm:

  chmod +x handout/connected
  ./handout/connected
  #     what:  msg_:"*$$*":
  #    where:  100.25.26.10
  # ... simulated network chatter ...
  # received: CMO{secret_code_v9hcdkd2}

  (Rebuild from source if preferred: cd source_code && make -j$(nproc))

If strings/nm/objdump are unavailable (e.g. Windows), carve printable runs:

  import re
  data = open('connected','rb').read()
  for m in re.finditer(rb'[ -~]{6,}', data):
      print(m.group().decode())


--------------------------------------------------------------------------------
 7. TOOLS USED
--------------------------------------------------------------------------------
  git                    Clone the challenge repository
  file                   Identify the binary (ELF64, PIE, not stripped)
  xxd / hexdump          Inspect ELF header, carve split flag immediates
  Python 3 (regex)       Pull printable strings (no binutils needed)
  Python 3               Reimplement checks + brute-force the message
  source (src/main.cpp)  Read exact algorithm & constants (shipped w/ challenge)
  WSL / Linux (optional) Run the ELF to confirm the flag end-to-end


--------------------------------------------------------------------------------
 8. KEY TAKEAWAYS
--------------------------------------------------------------------------------
  - Recover the algorithm, don't brute the output: the flag suffix is a pure
    function of the input message, so the challenge reduces to finding the one
    message that satisfies three readable constraints.
  - Constraint narrowing beats blind search: length 8 + palindrome + even +
    printable collapses to 4 free bytes (~5M); the checksum pins a single value.
  - The XOR-0x42 "encryption" through PC9 is a red herring -- it round-trips and
    cancels, so every helper sees plaintext.
  - The "logs"/"breached garage" story is flavor; the real gate is the target
    PC's three-way validation.

FINAL FLAG: CMO{secret_code_v9hcdkd2}
================================================================================
